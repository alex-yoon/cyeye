# cellmap

Cell image analysis pipeline

### Using the notebooks

Before running the notebooks, install in editable mode from the project root:

```bash
pip install -e .
```

This makes the code under `src/` available to the notebooks.